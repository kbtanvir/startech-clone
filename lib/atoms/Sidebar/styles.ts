import { StyleProps } from "@chakra-ui/react";

export const styles = {
  wrapper: {
    bg: "gray.300",
    minHeight: "100vh",
    placeItems: "start",
    placeContent: "space-between",
    w: "full",
    pt: "40px",
  } as StyleProps,
};
